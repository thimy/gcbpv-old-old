<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:page_title) || "Groupement Culturel Breton des Pays de Vilaine" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
    <link rel="shortcut icon" type="image/svg+xml" href="favicon.svg"/>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="<%= content_for(:page_class) || "" %>">
    <%= render(Global::HeaderComponent.new(title: "GCBPV", current_user: current_user)) %>
    <% if notice.present? %>
      <div class="full callout wrapper" data-notice="notice">
        <p><%= notice %></p>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="full callout wrapper" data-notice="error">
        <p><%= alert %></p>
      </div>
    <% end %>
    <main class="main-content">
      <%= yield %>
    </main>
    <%= render(Global::FooterComponent.new) %>
  </body>
</html>
